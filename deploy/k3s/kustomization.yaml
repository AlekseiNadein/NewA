apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: nav

resources:
  - namespace.yaml
  - storage.yaml
  - postgres.yaml
  - rabbitmq.yaml
  - redis.yaml
  - app.yaml
  - ingress.yaml

images:
  - name: nav-saas
    newName: nav-saas
    newTag: dev
